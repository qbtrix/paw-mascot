# Rust build output
app/src-tauri/target/
app/src-tauri/gen/

# bun
node_modules/
bun.lock

# web/_fx/ is deliberately NOT ignored. The avatar effect is vendored and
# committed so this repo builds on its own, with no sibling checkout required.
# `bun run sync-fx` refreshes it from ../paw-fx when that is present.
# See web/_fx/README.md.
